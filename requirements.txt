# ssl_detection_xray_v2 — Python dependencies
# Phase 0 pins are intentionally loose; exact versions captured in
# reports/phase0_pip_freeze.txt after 00_check_environment.py runs.
#
# Primary detection framework: MMDetection (OpenMMLab).
# Detectron2 is OPTIONAL and not installed by default (Windows build issues).

# --- Core scientific stack ---
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10

# --- Deep learning ---
# Install torch/torchvision matching your CUDA via the official index, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16

# --- MMDetection stack (primary framework) ---
# mmcv is typically installed via mim against the matching torch/CUDA:
#   pip install -U openmim && mim install mmengine "mmcv>=2.1" "mmdet>=3.3"
openmim>=0.3
mmengine>=0.10
mmcv>=2.1
mmdet>=3.3

# --- Imaging / medical IO ---
opencv-python>=4.8
pydicom>=2.4
Pillow>=10.0

# --- Detection metrics / annotations ---
pycocotools>=2.0.7

# --- Visualization & reporting ---
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66

# --- Config / utilities ---
PyYAML>=6.0
rich>=13.0

# --- Testing ---
pytest>=7.4

# --- OPTIONAL: Detectron2 (NOT default; manual install if desired) ---
# detectron2   # optional alternative framework; build manually per platform
